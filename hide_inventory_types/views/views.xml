<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <record id="hide_inventory_types_view_users_form" model="ir.ui.view">
            <field name="name">hide_inventory_types_view_users_form</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='companies_count']" position="after">
                    <field name="stock_type_ids" widget="many2many_tags"/>
                </xpath>

            </field>
        </record>

        <!-- Inherit Form View to Modify it -->
        <record id="hide_inventory_types_view_picking_type_form" model="ir.ui.view">
            <field name="name">hide_inventory_types_view_picking_type_form</field>
            <field name="model">stock.picking.type</field>
            <field name="inherit_id" ref="stock.view_picking_type_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='show_entire_packs']" position="after">
                    <field name="user_ids" widget="many2many_tags"/>
                </xpath>

            </field>
        </record>

    </data>
</odoo>